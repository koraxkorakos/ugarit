#pragma once
#include <ugarit/typealiases.hpp>
#include <ugarit/concepts.hpp>
namespace ugarit {

using std::uint64_t; // make 128


struct M {
    template <typename L, typename R, typename S = double>
    static S operator()(){

    }
};

struct MV {
    static constexpr bb(Metric M);
    template <uint64, Metric M>
    Arithmetic auto numericoperator(uint64) const;
};

}